{% extends 'Layout.html.twig' %}

{% block body %}
    <h1>Groups list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for group in groups %}
            <tr>
                <td><a href="{{ path('admin_groups_show', { 'id': group.id }) }}">{{ group.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('admin_groups_show', { 'id': group.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('admin_groups_edit', { 'id': group.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_groups_new') }}">Create a new group</a>
        </li>
    </ul>
{% endblock %}
